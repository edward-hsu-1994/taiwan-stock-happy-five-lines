name: Update stock data

on:
  schedule:
    # 16:00 Asia/Taipei (08:00 UTC), every day.
    - cron: '0 8 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: update-stock-data
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: scripts/requirements.txt

      - name: Install job dependencies
        run: python -m pip install -r scripts/requirements.txt

      - name: Fetch latest completed trading-day closes
        run: python scripts/fetch_stock_prices.py --mode latest

      - name: Commit and push data changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git diff --quiet -- public/data; then
            echo "No watchlist or stock data changes to commit."
            exit 0
          fi
          git add public/data
          git commit -m "chore(data): update daily stock prices"
          git push
